"""Rebuilding the noise model from a local CAMPFIRE archive.

Needs the `build` extra (astropy, scipy, pandas, matplotlib) and an archive on
disk. See `campfire-etc build --help` and etc/README.md for the run order.
"""
