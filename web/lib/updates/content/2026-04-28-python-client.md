---
title: "Python client & CLI for programmatic access"
date: 2026-04-28
category: client
summary: "The unified campfire Python package lets you query the catalog, download spectra, and script bulk workflows without the web UI."
links:
  - { label: "Programmatic access guide", href: "/docs/api" }
  - { label: "Python client reference", href: "/docs/api/python-client" }
  - { label: "CLI reference", href: "/docs/api/cli" }
---

The unified `campfire` Python package provides an API client and CLI for working
with the archive outside the web portal — query the catalog with coordinate and
metadata filters, pull individual spectra or bulk samples, and script repeatable
workflows.

Install it and authenticate with an API key from your
[profile](/profile), then jump into the
[getting-started guide](/docs/api/getting-started) for your first query and plot.

Full references are available for the [Python client](/docs/api/python-client)
and the [CLI](/docs/api/cli).
